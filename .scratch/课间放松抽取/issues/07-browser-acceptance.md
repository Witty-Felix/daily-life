# 07 — 真实浏览器端完整验收

**What to build:** 通过真实浏览器行为验证完整产品流程，而不是只验证内部函数或模拟数据。

**Blocked by:** 01 — 核心课间流程与加权抽取；02 — “换一个”与结果锁定；03 — 放松建议与戒色练习选择；04 — 贪吃蛇课间游戏；05 — 最近 7 天记录与本地数据设置；06 — 响应式、低干扰反馈与设置

**Status:** resolved

- [x] 开始课间并验证加权抽取流程
- [x] 验证进行中课间刷新后可恢复
- [x] 验证不能创建并行课间
- [x] 验证换一个最多一次且排除当前结果
- [x] 验证完成、未完成和结束课间记录
- [x] 验证贪吃蛇每次课间最多 3 局及中断计数
- [x] 验证边界穿越、方块碰撞和食物生成；自身碰撞由既有 `snakeGame` 回归测试覆盖
- [x] 验证电脑键盘和手机滑动操作
- [x] 验证最近 7 天统计、跨午夜归档和清除本地记录
- [x] 验证动画开关、减少动态效果和无声音行为
- [x] 验证最终结果满足 CONTEXT.md 中的领域规则

## Comments

### 2026-09-09 验收

Ticket 07 已实现并完成真实浏览器验收。新增 `e2e/browser-acceptance.spec.ts`，覆盖 8 组验收场景：预设权重边界、刷新恢复与禁止并行、换一个锁定、未完成记录、贪吃蛇地图/食物/边界/三局上限/中断计数/键盘、方块碰撞、手机滑动；既有 E2E 同时覆盖戒色子活动、最近 7 天统计、跨午夜归档、清除本地记录、动画/减少动态效果和无声音行为。自身碰撞由 `src/domain/snakeGame.test.ts` 的领域回归测试覆盖。

为修复验收中发现的真实问题，`ActiveBreak` 在恢复已有贪吃蛇局数时会将当前局视为已完成，避免刷新后或达到 3/3 后无法确认活动完成；同时为响应式 E2E 补充确定性随机值，并将 Playwright worker 固定为 1，避免 Windows 本地 Vite 测试服务器并发启动不稳定。

最终验证：`npm run test:all` 通过（typecheck、Vitest 5 个文件/31 tests、Vite build、Playwright 16 tests）；`git diff --check` 通过。
